team:
  leader: "张三"
  age: 32
  email: "njuit@qq.com"
  phone: "13900001111"
  createTime: "2019-01-01"


# Spring核心配置
spring:
  application:
    name: springboot-week04
  # 文件上传核心配置
  servlet:
    multipart:
      enabled: true # 开启文件上传功能，默认true
      max-file-size: 10MB # 单个文件最大大小限制，默认1MB
      max-request-size: 100MB # 单次请求（多文件）总大小限制，默认10MB

